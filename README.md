# Expense Tracker (Python)

## 📌 Project Description
This is a **beginner-level console-based Expense Tracker** built using Python.  
The program allows users to **add expenses** and **view saved expenses** using a CSV file for storage.

The data is stored permanently, so expenses remain saved even after the program is closed.

---

## 🚀 Features
- Add an expense (amount, category, description)
- Save expenses in a CSV file
- View all saved expenses
- Simple and beginner-friendly logic

---

## 🧠 Concepts Used
- Python functions
- File handling (`open`, `close`)
- CSV files
- User input
- Conditional statements
- `if __name__ == "__main__"`

---

## 🛠 Technologies Used
- Python 3
- CSV module (built-in)
- Visual Studio Code

---

## 📁 Project Structure
